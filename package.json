{
  "name": "chatgpt-rerun-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check background.js && node --check content.js && node --check control.js && node --check popup.js",
    "test": "node --test"
  }
}
