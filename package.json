{
  "name": "chatgpt-rerun-extension",
  "version": "2.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check background.js && node --check sandbox-fetch.js && node --check content.js && node --check goal.js && node --check popup.js",
    "test": "node --test tests/v2-*.test.mjs"
  }
}
