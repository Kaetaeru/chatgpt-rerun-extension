{
  "name": "chatgpt-rerun-extension",
  "version": "2.1.8",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check background.js && node --check content.js && node --check conversation-limit.js && node --check artifact-reader.js && node --check page-artifact-reader.js && node --check goal.js && node --check popup.js",
    "test": "node --test tests/v2-*.test.mjs"
  }
}
